apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-demo
  namespace: cka-koans
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resource-demo
  template:
    metadata:
      labels:
        app: resource-demo
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          resources:
            requests:
              cpu: "0"
              memory: "0Mi"
            limits:
              cpu: "0"
              memory: "0Mi"
